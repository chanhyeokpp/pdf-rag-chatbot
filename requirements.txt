# RAG 강의를 위한 필수 패키지 (poetry.lock 해석 결과와 일치 — Python 3.12 / 3.13)

# LangChain 핵심
langchain==0.3.27
langchain-community==0.3.31
langchain-openai==0.3.35
langchain-huggingface==0.1.2

# 문서 로딩 및 처리 (PDF 추출 4종)
pymupdf==1.27.1
pymupdf4llm==0.0.27
langchain-pymupdf4llm==0.4.1
docling==2.97.0          # AI 기반 표 구조 인식 추출 (로컬, 무료)

# 벡터 데이터베이스 및 검색
faiss-cpu==1.13.2
rank-bm25==0.2.2
kiwipiepy==0.21.0        # 한국어 형태소 분석 (BM25 토크나이저 심화 실습용)

# 챗봇 / 에이전트 (langchain 0.3.x 호환으로 0.2.x 계열 확정)
langgraph==0.2.76

# 배포
streamlit==1.58.0

# 임베딩 및 리랭킹
sentence-transformers==5.2.3
# transformers 5.x는 breaking change 있음 → 4.x 고정
transformers>=4.33,<5.0
torch>=2.1.0,<2.10.0

# 유틸리티
python-dotenv==1.2.1

# 노트북 실행 환경
jupyter>=1.0.0
nbconvert>=7.0.0

# (선택) 슬라이드 재생성용
python-pptx==1.0.2
